apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: cron-binding
  namespace: phase-v
spec:
  type: bindings.cron
  version: v1
  metadata:
  - name: schedule
    value: "*/5 * * * *"  # Every 5 minutes - adjust as needed
  - name: direction
    value: "input"
