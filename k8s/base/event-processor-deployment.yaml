apiVersion: apps/v1
kind: Deployment
metadata:
  name: phase-v-event-processor
  namespace: todo-app
  labels:
    app: phase-v-event-processor
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: phase-v-event-processor
  template:
    metadata:
      labels:
        app: phase-v-event-processor
        version: v1
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "phase-v-event-processor"
        dapr.io/app-port: "8001"
        dapr.io/config: "dapr-config"
        dapr.io/sidecar-memory-limit: "512Mi"
        dapr.io/sidecar-cpu-limit: "500m"
    spec:
      containers:
      - name: event-processor
        image: phase-v-event-processor:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8001
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: phase-v-config
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 5
          periodSeconds: 5
